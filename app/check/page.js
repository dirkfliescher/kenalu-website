import CheckTool from '@/components/blocks/CheckTool';

export const metadata = {
  title: 'Selbstcheck – kenalu',
  description: 'Wie hoch ist dein Veränderungsdruck? 6 Fragen, 2 Minuten, eine ehrliche Einschätzung.',
};

export default function CheckPage() {
  return <CheckTool />;
}
