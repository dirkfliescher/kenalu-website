import CheckTool from '@/components/blocks/CheckTool';

export const metadata = {
  title: 'AI Readiness Check – kenalu',
  description: 'Wo steht euer Unternehmen mit AI? 6 Fragen, 2 Minuten — und ihr seht, welche kenalu-Leistung zu eurer Situation passt.',
};

export default function CheckPage() {
  return <CheckTool />;
}
