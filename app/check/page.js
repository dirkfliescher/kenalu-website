import CheckTool from '@/components/blocks/CheckTool';

export const metadata = {
  title: 'AI Readiness Check – kenalu',
  description: 'Wo steht dein Unternehmen mit AI? 6 Fragen, 2 Minuten — und du siehst, welche kenalu-Leistung zu deiner Situation passt.',
};

export default function CheckPage() {
  return <CheckTool />;
}
